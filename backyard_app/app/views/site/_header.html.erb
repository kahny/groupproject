<div class="siteheader">
  <div class="container">
      <h1 id="sitetitle">backyard application</h1>
      <p id="sitedescrip">take advantage of other people's backyards</p>

            <%= form_for @search, method: :get, url: '/search', html: {class: "form-inline", id: 'search-bar-home'} do |f| %>


            <%= f.text_field :search, autofocus: true, placeholder: "San Francisco", class: "form-control searchval" %>

            <%= f.submit "Search now", class: "btn btn-default" %>

      <% end %>
  </div>
</div>

<h1><%= @loc%></h1>

 <!--    <div id="selectbar">
    <p id="selectbartext">Start by choosing a neighborhood:</p>
    <div id="select-bar-btn" class="btn-group btn-group-sm">
      <div class="btn-group">
        <button type="button" class="btn btn-default">Haight</button>
      </div>
      <div class="btn-group">
        <button type="button" class="btn btn-default">SoMa</button>
      </div>
      <div class="btn-group">
        <button type="button" class="btn btn-default">Nob Hill</button>
      </div>
   </div> -->
